# Survey — <what you were about to build>

<!-- Copy to docs/surveys/NNNN-<topic>.md. Check 17_prior_art requires one of these to
     change on any feat/ branch that adds public surface. Keep it short: the gate asks
     that the question was ASKED and ANSWERED on the record, not that the answer is long. -->

**Question.** In one sentence: what capability was needed?

## What already exists

| Where | What was found | Fit |
|---|---|---|
| This repo | `module.symbol` — what it does | reuse / extend / not a fit because … |
| A declared dependency | `package.thing` | … |
| Ecosystem | name + link (advisory — no key-free search exists; see ADR-0051) | … |

## Decision

Reuse `X` / extend `Y` / **build**, because … (one paragraph; the *because* is the point).

## Sources
- links, docs, the command you ran to look
